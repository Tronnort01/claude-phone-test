package com.stealthcalc.vault.model

/**
 * Sort order options for files shown in the Secure Media Vault.
 */
enum class VaultSortOrder {
    DATE_NEWEST,
    DATE_OLDEST,
    SIZE_LARGEST,
    SIZE_SMALLEST,
    NAME_AZ
}
