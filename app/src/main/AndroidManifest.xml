<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <!-- PARTIAL_WAKE_LOCK keeps the CPU + MediaRecorder/CameraX pipeline
         running while the device screen is off, so the fake lock cover
         and an accidental power-button press cannot interrupt a
         recording-in-progress. Released when stopRecording finishes. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Round 4 Feature A: lets the user exempt the app from Doze /
         battery optimization so the foreground recorder service isn't
         killed during long recordings. Prompted from Settings; the
         intent ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS opens
         Android's system dialog. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Round 4 Feature B: SYSTEM_ALERT_WINDOW lets OverlayLockService
         draw the fake-lock overlay on top of other apps / the home
         launcher. Used ONLY while a recording is in progress and ONLY
         if the user has opted in via Settings → "Use secure overlay
         lock". Requires the user to additionally grant the permission
         via Settings.ACTION_MANAGE_OVERLAY_PERMISSION — declaring in
         the manifest is necessary but not sufficient on API 23+. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Used to look up MediaStore URIs that match photo-picker content URIs,
         so that originals can be deleted after importing into the vault.
         See VaultViewModel.resolveToMediaStoreUri. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Round 5: vault export. On API 29+ MediaStore.insert grants write
         access implicitly via the returned URI, but on API 26-28 (legacy
         storage) writing into Pictures/StealthCalc still requires the
         classic write permission. Capped at 28 so it doesn't show up in
         the install dialog on modern devices. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".StealthCalcApp"
        android:label="Calculator"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:theme="@style/Theme.StealthCalc">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|smallestScreenSize|navigation|keyboard|density|fontScale">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".recorder.service.RecorderService"
            android:exported="false"
            android:foregroundServiceType="microphone|camera" />

        <!-- Round 4 Feature B: overlay lock service. NOT a foreground
             service — it runs only while the overlay view is attached,
             and the recorder's own foreground service keeps the process
             alive. Exported=false; no external caller should be able to
             pop our fake lock. -->
        <service
            android:name=".recorder.service.OverlayLockService"
            android:exported="false" />

        <receiver
            android:name=".core.notifications.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
